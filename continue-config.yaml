name: Colab Ollama
version: 1.0.0
schema: v1

models:
  - name: YOUR_SELECTED_MODEL
    provider: ollama
    model: YOUR_SELECTED_MODEL
    apiBase: YOUR_TAILSCALE_IP:11434

    defaultCompletionOptions:
      contextLength: 8192
      temperature: 0.2

    roles:
      - chat
      - edit
      - apply
      - autocomplete
